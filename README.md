# react-dnd

Simple react-hook abstraction over the native drag and drop interface. It handles adding/removing the event handlers and the neccesary logic for the provided ref to become draggable/droppable